import { Container } from "../../components";
import { BackButton } from "../../section/register";

const Register = () => {
  return (
    <Container>
      <BackButton />
    </Container>
  );
};

export default Register;
